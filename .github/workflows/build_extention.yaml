name: Build Extension
on: [push]
jobs:
  build-extension:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
      - run: npm install --global web-ext
      - run: web-ext build
        working-directory: /